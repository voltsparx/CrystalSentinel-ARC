[package]
name = "sentinel-policy"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sentinel-bio-response = { path = "../sentinel-bio-response" }
sentinel-common = { path = "../sentinel-common" }

[lib]
path = "src/lib.rs"

