# Deployment

Operational packaging and service manifests live here.

- `docker/`: container assets
- `kubernetes/`: cluster manifests and overlays
- `systemd/`: Linux service definitions
- `windows-service/`: Windows service installation assets

