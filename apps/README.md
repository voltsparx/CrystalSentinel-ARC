# Applications

Runtime binaries live here.

- `sentineld/`: long-running sensor, detection, and response daemon with sample,
  JSONL file, JSONL stdin, and runtime launch overrides for mesh, autonomy, and
  passive-only operation
- `sentinelctl/`: operator and admin CLI
- `sentinel-console-api/`: API surface for dashboards and integrations
